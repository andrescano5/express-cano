<!DOCTYPE html>
<html>
  <head>
    <title>Agregar Libro</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include('../partials/nav.ejs') %>

    <h1>Agregar Libro</h1>

    <form action="/agregar-libro" method="POST">
        <label>
            Título:
            <input type="text" name="titulo">
        </label><br>
        <label>
            Precio:
            <input type="number" name="precio">
        </label><br>
        <label>
            Portada:
            <input type="text" name="portada" value="test">
        </label><br>

        <!-- Al usuario le muestro "king" pero a la DB le envio el id a través del atributo "value". -->
      
        <label for="autor">Autor:</label>
        
        <select id="autor" name="autor">
            <% authors.forEach( autor => { %>
            <option value="<%= autor.idAutor %>">
              <%= autor.nombreCompleto %>
            </option>
            <% }) %> 
        </select>
        
        <br>

        <input type="submit" value="Agregar Libro">
    </form>
  
  </body>
</html>